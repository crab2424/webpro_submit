<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>星座</title>
    </head>
    <body>
        <h2>星座一覧</h2>
        <table>
            <tr>
                <th>id</th>
                <th>星座名</th>
                <th>英語表記</th>
            </tr>
            <% let id = 1 %> <% for( let row of data.slice(1) ) { %>
            <tr>
                <td><%= id %></td>
                <td><a href="/stella/<%= id %>"><%= row.name %></a></td>
                <td><%= row.en %></td>
            </tr>
            <% id += 1 %> <% } %>
        </table>
        <a href="/public/stella_new.html">追加</a><br>
        <a href="/">トップへ戻る</a>
    </body>
</html>
