<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>星座</title>
        <link rel="stylesheet" href="../../public/style.css">
    </head>
    <body>
        <h2>星座一覧</h2>
        <table class="table-show">
            <tr>
                <th>id</th>
                <th>星座名</th>
                <th>英語表記</th>
            </tr>
            <% data.forEach( (row) => { %>
                <% if (row !== null && row.id !== 0) { %>
            <tr>
                <td><%= row.id %></td>
                <td><a href="/stella/<%= row.id %>"><%= row.name %></a></td>
                <td><%= row.en %></td>
            </tr>
            <% } %>
            <% }); %>
        </table>
        <a href="/public/stella_new.html">追加</a>
        <a href="/" style="margin-left: 30px;">トップへ戻る</a>
    </body>
</html>
